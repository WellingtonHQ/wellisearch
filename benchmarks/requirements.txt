# Benchmark dependencies.
#
# The app itself uses FastEmbed; the embedding-bench deps below are
# benchmark-only and are NOT declared in pyproject.toml on purpose.
#
# Tip (Linux): to avoid the multi-GB CUDA torch wheel, install the CPU build
# first, then the rest:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers
#
# Or just:  pip install -r benchmarks/requirements.txt   (pulls default torch)

# --- Embedding benchmark (embed_bench.py) — CPU inference only ---
torch>=2.2
sentence-transformers>=3.0
einops>=0.7
# Production baseline (all-MiniLM) is benchmarked through FastEmbed/ONNX,
# matching the app's runtime.
fastembed>=0.4

# --- LLM fit-markdown cleanup benchmark (llm_cleanup_bench.py) ---
# A thin HTTP client: httpx for the OpenAI-compatible calls, psycopg to read
# the sample from Postgres. Both are already app deps (pyproject.toml); listed
# here so the bench is self-contained.
httpx>=0.27
psycopg[binary]>=3.1
